<div class="marquee-wrap">
  <div class="marquee-track">
    {% for i in (1..2) %}
      <span>Éclat Face Cream</span><span class="dot">✦</span>
      <span>Mish Lip Scrub</span><span class="dot">✦</span>
      <span>Light Yourself First Luminary</span><span class="dot">✦</span>
      <span>{{ section.settings.launch_text }}</span><span class="dot">✦</span>
      <span>Luxury For The Busy Woman</span><span class="dot">✦</span>
    {% endfor %}
  </div>
</div>

{% schema %}
{
  "name": "Marquee",
  "tag": "section",
  "class": "section-marquee",
  "settings": [
    { "type": "text", "id": "launch_text", "label": "Launch text in strip", "default": "Launching Feb 25" }
  ],
  "presets": [{ "name": "Marquee" }]
}
{% endschema %}
